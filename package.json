{
  "name": "jira-control-chart",
  "version": "1.0.0",
  "private": true,
  "description": "CPAO cycle-time control chart, built from the Jira REST API and published on GitHub Pages",
  "scripts": {
    "fetch": "node fetch-issues.js && node fetch-changelogs.js",
    "build": "node build.js",
    "test": "node test.js",
    "refresh": "npm run fetch && npm run build && npm test"
  },
  "engines": {
    "node": ">=18"
  }
}
